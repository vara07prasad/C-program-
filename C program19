#include <stdio.h>
#include <math.h>

int main() {
    int num, sq;

    printf("Enter a number: ");
    scanf("%d", &num);

    sq = sqrt(num);

    if(sq * sq == num)
        printf("%d is a Perfect Square\n", num);
    else
        printf("%d is NOT a Perfect Square\n", num);

    return 0;
}
